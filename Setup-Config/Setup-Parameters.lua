-- The name of the VS Project. Regenerate the solution if you want to change the name.
SolutionName = "Library"

-- Folder name that the code base is held in. This must be confirmed before generating the solution.
MainFolder = "Source"
